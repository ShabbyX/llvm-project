import("//build_overrides/llvm.gni")

static_library("Debuginfod") {
  output_name = "LLVMDebuginfod"
  deps = [
    "//${llvm_build_root}/build/libs/curl",
    "//${llvm_build_root}/secondary/llvm/lib/DebugInfo/Symbolize",
    "//${llvm_build_root}/secondary/llvm/lib/Support",
  ]
  sources = [
    "BuildIDFetcher.cpp",
    "Debuginfod.cpp",
    "HTTPClient.cpp",
    "HTTPServer.cpp",
  ]
  configs += llvm_code_override_configs_append
  configs -= llvm_code_override_configs_remove
}
